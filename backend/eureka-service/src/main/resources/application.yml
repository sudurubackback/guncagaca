spring:
  application:
    name: eureka
  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:8888
server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    org.hibernate.type: trace
