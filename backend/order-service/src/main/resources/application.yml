server:
  port: 8083

spring:
  application:
    name: order-service
  datasource:
    url: jdbc:mysql://15.165.116.170:3306/order_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: coco
    password: d102d102

  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    properties:
      hibernate:
        format_sql: true
    show-sql: true